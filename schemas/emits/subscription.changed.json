{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "subscription.changed",
  "description": "User subscription plan or status changed.",
  "type": "object",
  "required": [
    "user_id",
    "plan",
    "status",
    "cancel_at_period_end"
  ],
  "properties": {
    "user_id": {
      "type": "string",
      "format": "uuid"
    },
    "plan": {
      "type": "string"
    },
    "status": {
      "type": "string"
    },
    "current_period_end": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "End of the current billing period (renewal/expiry moment), populated from the provider subscription object. null means not yet known (e.g. the checkout.session.completed milestone, before the customer.subscription.created event lands) \u2014 never a fabricated zero timestamp."
    },
    "cancel_at_period_end": {
      "type": "boolean",
      "description": "True when the subscriber has cancelled and the subscription runs to the end of the paid period without renewing. Not inferable from `status`, which stays 'active' for the whole remaining period \u2014 a consumer reading only `status` cannot tell a live subscription from one that is ending, which is how paying customers reached a cancellation date nobody had told them about. Always emitted."
    }
  },
  "additionalProperties": true
}
