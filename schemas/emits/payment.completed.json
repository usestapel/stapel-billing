{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "payment.completed",
  "description": "A payment transaction completed successfully.",
  "type": "object",
  "required": [
    "user_id",
    "amount_cents",
    "currency",
    "transaction_id",
    "created_at"
  ],
  "properties": {
    "user_id": {
      "type": "string",
      "format": "uuid"
    },
    "amount_cents": {
      "type": "integer"
    },
    "currency": {
      "type": "string"
    },
    "transaction_id": {
      "type": "string",
      "format": "uuid"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "package": {
      "type": "string"
    },
    "plan": {
      "type": "string"
    },
    "invoice_url": {
      "type": "string",
      "description": "Provider-hosted invoice or receipt document for this payment, when the payload carried one. Absent for checkout-session payments, whose payload has no document URL."
    },
    "period_start": {
      "type": "string",
      "format": "date-time",
      "description": "Start of the service period this payment covers. Present for subscription renewals; absent for one-off package purchases, which cover no period."
    },
    "period_end": {
      "type": "string",
      "format": "date-time",
      "description": "End of the service period this payment covers. Present and absent under the same conditions as period_start; the two are always emitted together or not at all."
    },
    "simulated": {
      "type": "boolean",
      "description": "TRUE when this payment was SIMULATED by a staff member rather than taken from a card \u2014 the staff mock-payment path (services.simulate_checkout_completed). Absent or false means real money. Any consumer that counts revenue, reports a conversion to an advertising platform, or writes to a customer about money they spent MUST exclude it: the account is a real person's, so an id-shaped test cannot be used to tell the two apart."
    }
  },
  "additionalProperties": true
}
